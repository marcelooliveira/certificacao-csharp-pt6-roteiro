# Como você deve completar o código relevante?

Você está desenvolvendo uma coleção personalizada chamada **LoanCollection** para uma classe chamada **Loan**.
Você precisa garantir que você pode processar cada objeto de empréstimo na coleção LoanCollection
usando um loop *foreach*.
Como você deve completar o código relevante? (Para responder, arraste o código apropriado
segmentos para os locais corretos na área de resposta. Cada segmento de código pode ser usado
uma vez, mais de uma vez ou nada. Pode ser necessário arrastar a barra de divisão entre painéis ou
desloque-se para ver o conteúdo.)

### Opções:

```
: IComparable
```
```
: IEnumerable
```
```
: IDisposable
```
```
public IEnumerator GetEnumerator()
```
```
public int CompareIo(object obj)
```
```
public void Dispose()
```
```
_loanCollection[0].Amount++;
```
```
return obj == null ? 1 : _loanCollection.Length;
```
```
return _loanCollection.GetEnumerator(};
```


### Código: 

```
public class LoanCollection [LACUNA 1]
{
    private readonly Loan[] _loanCollection;
    public LoanCollection(loan[] loanArray)
    {
        _loanCollection = new Loan[loanArray.Length];
        for (int i = 0; i < loanArray.Length; i++)
        {
            _loanCollection[i] = loanArray[i];
        }
    }

    [LACUNA 2]
    {
        [LACUNA 3]
    }
}
```

### Resposta:

```
: IEnumerable
```
```
public IEnumerator GetEnumerator()
```
```
return _loanCollection.GetEnumerator(};
```
