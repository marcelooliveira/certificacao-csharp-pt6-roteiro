# preencha cada declaração de acordo com as informações apresentadas no código.

Você tem o seguinte código:

```csharp
private static Dictionary<string, int> CreateTestData()
{
    Dictionary<string, int> dict = new Dictionary<string, int>()
    {
        {"Accounting", 1},
        {"Marketing", 2},
        {"Operatlons", 3}
    };
    return dict;
}

private static bool FindInList(string searchTerm)
{
    Dictionary<string, int> data = CreateTestData();

    if (data.ContainsKey(searchTerm))
    {
        return true;
    }
    else
    {
        return false;
    }
}
```

Para responder, preencha cada declaração de acordo com as informações apresentadas no código.

Se o texto de busca é "Finance", o resultado será [false|true|null]
Se o texto de busca é "1", o resultado será [false|true|null]
Se o texto de busca é "Operations", o resultado será [false|true|null]

### Resposta:

false
false
true